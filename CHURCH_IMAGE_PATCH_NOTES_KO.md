# 예배 소식 사진 업로드 패치

## 적용 내용
- `/admin` 예배 소식 등록에 대표 사진 선택 추가
- 사진은 브라우저에서 최대 1600px로 축소 후 WebP 품질 82%로 압축
- Supabase Storage 버킷 이름은 정확히 `교회 이미지` 사용
- 저장 경로는 `예배소식ID.webp` 형식
- 홈페이지 최신 5개 예배 소식 카드에 등록 사진 표시
- 사진이 없는 기존 소식은 기존 기본 이미지 자동 표시
- 예배 소식 삭제 시 연결 사진도 함께 삭제
- 30일 지난 예배 소식 일괄 삭제 시 연결 사진도 함께 정리
- 헤더 로고, PWA, SEO, 주보, 설교 구조는 변경하지 않음

## 최초 1회 필요
Supabase SQL Editor에서 `SUPABASE_CHURCH_IMAGES_SETUP.sql` 전체를 실행하세요.
이 SQL은 `교회 이미지` 버킷만 공개 읽기 가능하게 하고, 로그인한 관리자에게 업로드/수정/삭제 권한을 줍니다.
