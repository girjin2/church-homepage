# V5 홈페이지 반영

- 사용자가 다시 올린 `대한예수교장로회 / 서재교회` 로고 이미지를 그대로 헤더에 사용
- PPT의 서재교회 건물 원본 이미지를 별도 레이어로 배치해 위/아래가 잘리지 않도록 수정
- 메인 버튼: `예배시간 보기`, `유튜브`
- 홈의 `예배 안내`를 `예배 소식`으로 교체
- Supabase `notices` 최신 5개를 홈에 카드 형태로 자동 표시
- 새 소식을 등록하면 가장 오래된 카드가 밀려나고 최신 5개가 계속 표시됨
- 관리자에서도 해당 영역을 `예배 소식`으로 표시
- 기존 Supabase/주보/설교/SEO/관리자 정리 기능 유지
